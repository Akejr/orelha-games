/**
 * Ponte para o pacote compartilhado (`/shared`). Mantém os imports do servidor
 * curtos e deixa explícito qual é a fronteira de código comum com o cliente.
 */
export * from '../../shared/src/index';
